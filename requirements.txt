# Lightweight dependencies for local development (use Docker for full stack)

# Data processing
pandas
numpy
requests

# Config
python-dotenv
pyyaml

# Logging
python-json-logger
structlog

# Testing
pytest
pytest-cov
pytest-asyncio
pytest-mock

# Utils
python-dateutil
pytz

# NOTE:
# - For the full Airflow + BigQuery stack, use `requirements-docker.txt` in the Docker build.
# - Installing `apache-airflow` and GCP libs locally on Windows can be problematic; prefer Docker.
